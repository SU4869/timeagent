# TimeAgent 测试操作手册

> 适用版本：commit `2d21e8f` 之后（含首页入口 / ICS 导入 / 自动周报 / 统计可视化 / 通知设置）
> 用途：① 改完代码用自动化测试回归；② 部署/装机后对照手动验证；③ 部署核对与常见排查。

---

## 0. 环境准备（只需一次）

| 项目 | 路径 |
|---|---|
| Node（managed，必须 22+） | `C:\Users\苏浚铭\.workbuddy\binaries\node\versions\22.22.2\node.exe` |
| jsdom | `C:\Users\苏浚铭\.workbuddy\binaries\node\workspace\node_modules` |
| 测试目录 | `TimeAgent_Web` 的**同级** `tests/`（`test_home.smoke.js` / `test_insight_one.js`） |

> 注意：jsdom 在 managed workspace 下，Node 20 跑会报 `ERR_REQUIRE_ESM`，必须用 22.22.2。
> `tests/` 当前在工程目录外、Git 不跟踪，换机器需手动带上。

---

## 1. 自动化测试（首选回归手段）

### 1.1 运行命令
在 `tests/` 目录下，开 Git Bash：

```bash
cd "C:/Users/苏浚铭/WorkBuddy/2026-08-10-03-54-26/tests"
NODE_PATH=C:/Users/苏浚铭/.workbuddy/binaries/node/workspace/node_modules \
C:/Users/苏浚铭/.workbuddy/binaries/node/versions/22.22.2/node.exe test_home.smoke.js
```

两个文件分别跑一遍（`test_home.smoke.js` 是主回归，`test_insight_one.js` 是 AI 日报/洞察专项）。

### 1.2 读结果
末尾输出形如：

```
结果：156 通过 / 0 失败
结果：11 通过 / 0 失败
```

有失败时先看是哪一组（A~V 字母分组），再定位对应断言。

### 1.3 已知“假阳性”与注意事项
- **时间敏感断言**：seed 数据按「今天」动态生成。若在北京时间 22:00 之后跑，全部 seed 日程已过期，冒烟里「实时建议」会走“已过/补一下”分支——已在正则里放宽（`|已过|未打卡|补一下`），正常不影响通过。
- **在线用例需带 apiKey**：`makeApp` 用 `seedOverride` 时，`apiKey` 参数会被忽略，必须在 `seedOverride` 里带 `apiKey` 才会走 AI 路径（H/N 组）。
- **jsdom 限制**：无 `TextEncoder/TextDecoder`（测试里从 `util` 注入）、`Blob` 无 `.text()`（用 `FileReader`）、`window.crypto` 只读（用 `webcrypto` 覆盖）。**不要为了跑通而改 app.js 去适配 jsdom**，只能在测试里 shim。

### 1.4 加新断言
测试通过 `window.__ta*` 钩子驱动真实 UI。要加断言先确认 app.js 是否已暴露对应钩子（搜 `window.__ta`），没有就在对应函数末尾 `init()` 里加一行 `window.__taXxx = {...}`。

---

## 2. 功能验证清单（手动）

> 用法：每项的「入口 → 操作 → 预期」三步对照。✅= 通过，❌= 异常（截图+描述反馈我）。

### 2.1 首页
| 功能 | 入口 | 操作 | 预期 |
|---|---|---|---|
| AI 洞察卡 | 首页顶部「AI 智能分析」 | 配了 Key 后等 ~1s | 摘要先显示离线再无缝换成 AI 文案，徽标变「AI 在线」 |
| 今日焦点条 | 首页 scopeBar 下 | 有今天的日程时看 | 显示「正在进行 / 下一项倒计时 / 空闲 / 过期未打卡」之一 |
| 一句话排期 | 首页快捷输入条 | 输入“明天9点健身”回车 | 自动打开规划浮层并发送，生成日程 |
| 冲突/过载预警 | 首页（有冲突时） | 故意加重叠日程 | 黄色预警条 + 点开看方案 |
| 趋势环比 | 首页分析卡 | 切到周/月视图 | 显示本周 vs 上周时长差值（小基数显示绝对值） |
| 行动仪表盘 | 首页分析卡 | 制造过载/深夜任务 | 出现「安排休息 / 调早」等行动卡，点按钮预填规划指令 |
| 优化今日入口 | 首页「AI 优化今日」下方 | 点「优化今日」 | 弹出重排方案，可应用 |
| 报告/周复盘入口 | 同上行 | 点「生成报告」「周复盘」 | 分别打开日报 / 周复盘浮层 |

### 2.2 日程（添加 / 编辑 / 删除）
| 功能 | 操作 | 预期 |
|---|---|---|
| 添加（表单） | 首页 + 或快捷条 | 出现带优先级/标签的表单，提交入列表 |
| 编辑 | 点日程条目 | 弹出编辑（含优先级段、标签选择），保存生效 |
| 删除 + 撤销 | 删除一条 | toast 带「撤销」，6s 内可恢复（保留原 id） |
| 打卡 + 撤销 | 点打卡 | 触感振动（安卓），toast 可撤销 |
| 重复日程 | 添加时勾重复 | 按次完成（isDone 按日期判断），删除可选删系列 |
| 冲突多方案 | 添加撞车日程 | 弹层给 A 提前 / B 推后 / C 挪走 三方案 + 自己来 |
| 去重防护 | 加同日同名 | 弹「发现重复日程」确认；AI 排期自动跳过 |

### 2.3 统计页
| 功能 | 操作 | 预期 |
|---|---|---|
| AI 解读 | 进统计页 | 报告卡有「本地规则 → AI 在线」徽标切换 |
| 大类汇总 | 统计页 | 自定义标签归入正经大类（学习/工作…），显示时长+占比 |
| 习惯热力图 | 统计页底部 | 119 天网格 + 4 级色阶 + 最长连续打卡 |
| 趋势折线图 | 统计页（有数据） | 14 天完成时长 SVG 折线 + 面积 + 柱 |
| 大类环图 | 统计页（有数据） | 环形图 + 图例，占比和 |
| 日/周/月切换 | scopeBar | 所有统计随范围重算 |

### 2.4 对话舱（我的 → 对话）
| 功能 | 操作 | 预期 |
|---|---|---|
| 上下文感知 | 问“我今天有什么安排” | AI 能答真实日程（严禁臆造） |
| 结构化操作 | 说“删除晨会” | 真实执行删除并刷新 |
| 离线意图 | 无 Key 时问“明天有什么/完成了多少/把X改到8点” | 本地规则正确应答（查询统计需在“完成”前识别） |
| 人格调教 | 说“以后说话可爱点” | 风格累积，回复确认；「恢复默认」清空 |
| 记忆范围 | 头部「记忆:本周」 | 可切 仅当日/本周/本月/全局/自定义 |
| 智能提示 | 空对话 | 显示引导 chips，点选填入输入框 |
| 主动聊天 | 静置 / 触发条件 | 催办 / 合理性点评 / 小提问，带「去看看」 |
| 反馈自学习 | 主动消息下点「有用/没用」 | 连续👎该类型暂停；👍×3 放宽上限 |

### 2.5 目标系统（我的 → 目标管理）
| 功能 | 操作 | 预期 |
|---|---|---|
| 设目标 | 面板新增「每天学习1小时」 | 列表显示进度条，达标✅ |
| 目标进度 | 对话问“目标进度” | 报告各大类投入 |
| 完成度预测 | 问“预测今天能完成多少” | 按近 14 天基线 × 剩余项数 |
| 目标守护 | 有未达标+空闲 | 主动消息提醒安排 |

### 2.6 提醒与通知
| 功能 | 操作 | 预期 |
|---|---|---|
| 动态提醒 | 设提醒的日程到点 | 提前量 = 固定 + 你平均迟到补偿 |
| 提醒进对话舱 | 提醒触发时对话舱已开 | 直接插入消息；未开则 toast「查看」 |
| 通知方式设置 | 我的 → 偏好 | 三档：仅应用内 / 系统通知 / 两者 |
| 测试通知 | 偏好页点「发送测试通知」 | 请求权限→弹系统通知（TWA 可能不弹，见 §5）；始终有 toast 兜底 |

### 2.7 数据与备份
| 功能 | 入口 | 操作 | 预期 |
|---|---|---|---|
| CSV 导出 | 我的 → 导出/导入 | 导出 | 下载带 BOM 的 CSV（Excel 不乱码） |
| iCal 导出 | 同上 | 导出 | `.ics` 可被日历导入 |
| **ICS 导入** | 同上（新） | 选 `.ics` 文件 | 解析 VEVENT，去重后加入日程，toast 显示 新增/跳过 |
| 加密备份 | 同上 | 设口令导出 → 导入 | PBKDF2+AES-GCM；错口令提示“口令不正确” |
| 模板 | 首页「和昨天一样」/ 我的模板面板 | 复制昨日 / 存模板 | 同日同名去重 / 冲突顺延 |
| 新手引导 | 清数据首次启动 | 看引导卡 | 3 张亮点 + 注入示例数据 |
| 备份到期提醒 | 静置 | 日程≥3 且久未备份 | 主动消息提醒备份 |

---

## 3. 调试钩子速查（`window.__ta*`）

浏览器/WebView 控制台可直接调用，便于验证与排错：

| 钩子 | 暴露内容 | 用途 |
|---|---|---|
| `__taImport` | `parseICS`, `importICSItems` | 单测/验证 ICS 解析 |
| `__taWeekly` | `autoWeeklyReview`, `clearAuto`, `weekKey` | 验证自动周报（同周/空数据跳过） |
| `__taNotify` | `sysNotifyAllowed`, `testNotify` | 验证通知门控 |
| `__taProactive` | 主动聊天触发/调度 | 验证催办/点评/提问 |
| `__taFb` | `fbMuted`, `fbBoosted`, `fbRecord`, `proactivePick`… | 验证👍👎自学习 |
| `__taBk` | `buildBackupFile`, `parseBackupText`, `applyBackupData` | 验证加密备份 |
| `__taOnboard` / `__taTpl` / `__taExport` / `__taHeat` / `__taRemind` | 各模块入口 | 验证对应功能 |
| `__taBack` | 返回栈处理 | 验证二级页返回（见壳侧滑修复） |

> 完整列表：`app.js` 里搜 `window.__ta`，每个功能模块 `init()` 末尾基本都挂了。

---

## 4. 部署核对（Push 之后）

1. **GitHub Desktop** 点 Push origin（偶发 500 是 GitHub 服务端，重试即可，本地 commit 不丢）。
2. **Netlify** 自动触发 build，约 1–2 分钟完成。
3. **手机**：后台划掉 TimeAgent 重开；若仍像没更新 → 设置→应用→TimeAgent→存储→清除缓存。
4. **线上核对**（可选，确认真部署）：
   ```bash
   # 本地 commit 的 app.js blob 大小
   git ls-tree 2d21e8f app.js        # 拿 blob SHA
   # 线上文件大小（应与 GitHub 文件一致）
   curl -sIL https://cheerful-vacherin-eea028.netlify.app/app.js | grep -i content-length
   # GitHub 真实 tip
   git ls-remote origin
   ```
   三者一致 = 部署到位。

---

## 5. 常见排查

| 现象 | 原因 | 处理 |
|---|---|---|
| 新功能看不到 | WebView 缓存命中旧版 | 清缓存 / 重启 App |
| 推送报 Internal Server Error | GitHub 服务端 500 | 等 2–3 分钟重试；命令行 `git push origin master` 亦可 |
| AI 不工作 | 没填 Key / 网络拦截 | 我的页填 DeepSeek Key；看 `apiReady()` 是否 true |
| 系统通知不弹 | TWA 在中国 ROM 侧载下被静默拦截 | 已知限制，用「仅应用内」toast 兜底；测试通知也走 toast |
| 侧滑返回直接退 App | 二级整页跳转 | 已修复（`onBackPressed` 优先 `goBack`）；重打包安装后验证 |
| 顶条白/黑缝隙（深浅切换） | 原生壳不知网页主题 | 已用 `onPageFinished`+MutationObserver 同步；重装 APK |
| 软键盘挡字 | edge-to-edge + 未消费 IME inset | 已修（Manifest adjustResize + inset 监听）；重装 APK |
| 自动化测试 Node 报 ESM 错 | 用了 Node 20 | 必须切 22.22.2 |

---

## 6. 提交/版本说明

- 每次大改后：`node --check app.js` 语法 → 跑两个测试 → 提交 → GitHub Desktop Push。
- 当前未版本化的：`tests/` 目录（在工程外）与本文档。若要随仓库走，把 `tests/` 移入 `TimeAgent_Web` 并提交。
